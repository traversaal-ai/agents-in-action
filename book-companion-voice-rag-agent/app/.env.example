# Example environment variables for tutorial purposes
# These are the actual values used by the application (public/safe to share)
VITE_SUPABASE_PROJECT_ID="your-supabase-project-id"
VITE_SUPABASE_PUBLISHABLE_KEY="your-supabase-publishable-key"
VITE_SUPABASE_URL="https://your-project-ref.supabase.co"

# Private webhook URL (set this as a Supabase secret via edge function secrets)
# This should NOT be in your .env file for security
N8N_RAG_URL="your-private-n8n-webhook-url"